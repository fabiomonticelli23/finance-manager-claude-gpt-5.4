---
phase: 01-portfolio-domain-valuation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - backend/package.json
  - backend/tsconfig.json
  - backend/vitest.config.ts
  - backend/src/main.ts
  - backend/src/app.module.ts
  - backend/src/health.controller.ts
  - backend/test/app.integration.spec.ts
autonomous: true
requirements:
  - PORT-01
must_haves:
  truths:
    - "Phase 1 can expose one trustworthy backend entrypoint that later settings and portfolio reads use as the canonical portfolio service boundary."
    - "User-driven base-currency changes and portfolio reads are protected by a backend validation boundary before they can influence persisted settings or canonical responses."
    - "The first runnable HTTP slice proves the portfolio application can serve validated requests before later exact-valuation behavior is layered on."
  artifacts:
    - path: "package.json"
      provides: "Root workspace scripts for backend, UI, and e2e validation"
    - path: "backend/src/main.ts"
      provides: "Nest bootstrap with global request validation"
    - path: "backend/test/app.integration.spec.ts"
      provides: "Backend test harness proving the scaffold runs"
  key_links:
    - from: "package.json"
      to: "backend/package.json"
      via: "workspace script delegation"
      pattern: "test:backend"
    - from: "backend/src/main.ts"
      to: "backend/src/app.module.ts"
      via: "NestFactory.create"
      pattern: "NestFactory\.create\(AppModule\)"
---

<objective>
Create the backend execution foundation for Phase 1 so later plans can add exact-valuation domain logic without first inventing project structure.

Purpose: Establish the first runnable backend workspace, validation boundary, and backend test harness for the canonical portfolio system.
Output: Root scripts, Nest backend scaffold, and a passing backend integration test baseline.
</objective>

<execution_context>
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-CONTEXT.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-RESEARCH.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and prove the backend workspace foundation</name>
  <read_first>
    - package.json
    - backend/package.json
    - .planning/ROADMAP.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-RESEARCH.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
  </read_first>
  <files>package.json, backend/package.json, backend/tsconfig.json, backend/vitest.config.ts, backend/src/main.ts, backend/src/app.module.ts, backend/src/health.controller.ts, backend/test/app.integration.spec.ts</files>
  <behavior>
    - Test 1: `GET /health` returns HTTP 200 with body `{ status: 'ok' }`.
    - Test 2: `npm run test:backend -- --runInBand` runs once without watch mode and exits cleanly.
    - Test 3: The backend boot path includes a global `ValidationPipe` before later settings and portfolio routes are added.
  </behavior>
  <action>Create the initial npm workspace rooted at `package.json` with workspaces `backend` and `frontend` and scripts exactly named `dev:backend`, `dev:frontend`, `test:backend`, `test:ui`, and `test:e2e`. Create a minimal Nest backend package in `backend/package.json` using NestJS 11 and TypeScript tooling, add `backend/src/main.ts` that boots `AppModule` with a global `ValidationPipe`, and add `backend/src/app.module.ts` plus `backend/src/health.controller.ts` so the backend exposes `GET /health` returning `{ status: 'ok' }`. In the same task, create `backend/vitest.config.ts` and `backend/test/app.integration.spec.ts` so `npm run test:backend -- --runInBand` boots the Nest app through Supertest and proves the scaffold is runnable. Keep the bootstrap thin and do not place any portfolio math here; this plan only establishes the execution surface required by later D-03 app-wide settings and canonical portfolio reads.</action>
  <verify>
    <automated>npm run test:backend -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains scripts `test:backend`, `test:ui`, and `test:e2e`.
    - `backend/src/main.ts` contains `ValidationPipe` and `NestFactory.create(AppModule)`.
    - `backend/src/health.controller.ts` contains `@Get('health')` and returns `{ status: 'ok' }`.
    - `backend/test/app.integration.spec.ts` imports Supertest and asserts `GET /health`.
    - `npm run test:backend -- --runInBand` exits 0.
  </acceptance_criteria>
  <done>Workspace scripts exist, the backend boots through `AppModule`, requests cross a validation boundary, and automated integration coverage proves the Phase 1 backend foundation is runnable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local client → Nest HTTP API | Untrusted request payloads enter the backend here |
| npm workspace scripts → backend test runner | Incorrect script wiring can hide failing tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `backend/src/main.ts` | mitigate | Register `ValidationPipe` globally at bootstrap so later settings and portfolio routes validate inputs at entry. |
| T-01-02 | D | root/backend test scripts | mitigate | Create explicit non-watch `test:backend` command and prove it in CI-style execution with `--runInBand`. |
| T-01-03 | I | `backend/src/health.controller.ts` | accept | Health response exposes only `{ status: 'ok' }`, so disclosure impact is negligible in local-first v1. |
</threat_model>

<verification>
Run `npm run test:backend -- --runInBand`. Confirm the command exits once, reports the health test as passing, and does not enter watch mode.
</verification>

<success_criteria>
- Root workspace scripts exist for backend, UI, and e2e testing.
- The Nest backend boots with global validation enabled.
- Backend integration tests pass from the root command surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-domain-valuation-foundation/01-01-SUMMARY.md`
</output>
