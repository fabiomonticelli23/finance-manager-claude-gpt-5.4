---
phase: 01-portfolio-domain-valuation-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-04
files_modified:
  - frontend/src/App.tsx
  - frontend/src/features/settings/components/BaseCurrencyForm.tsx
  - frontend/src/features/portfolio/components/PortfolioSummaryCard.tsx
  - frontend/src/features/portfolio/components/PortfolioSection.tsx
  - frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx
  - frontend/src/pages/PortfolioPage.tsx
  - frontend/src/pages/PortfolioPage.test.tsx
  - e2e/package.json
  - e2e/playwright.config.ts
  - e2e/tests/portfolio.spec.ts
autonomous: false
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
must_haves:
  truths:
    - "User can open one portfolio page that shows the selected base currency and visible total net worth in that currency, plus accounts and holdings in the UI order defined by the UI spec."
    - "User can save the app-wide base currency from the dedicated settings card and see canonical backend data refresh."
    - "Relevant rows show native and converted values together by default and can reveal one lightweight valuation breakdown at a time."
  artifacts:
    - path: "frontend/src/pages/PortfolioPage.tsx"
      provides: "Phase 1 portfolio screen composed from canonical backend data"
    - path: "frontend/src/features/settings/components/BaseCurrencyForm.tsx"
      provides: "App-wide base currency control with Save base currency CTA"
    - path: "frontend/src/features/portfolio/components/PortfolioSummaryCard.tsx"
      provides: "Visible net-worth summary card bound to the selected base currency"
    - path: "frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx"
      provides: "On-demand row transparency interaction"
    - path: "e2e/tests/portfolio.spec.ts"
      provides: "End-to-end proof of UI flow and backend-driven refresh"
  key_links:
    - from: "frontend/src/features/settings/components/BaseCurrencyForm.tsx"
      to: "frontend/src/api/settings.ts"
      via: "React Hook Form submit mutation"
      pattern: "saveBaseCurrency|updateBaseCurrency"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/PortfolioPage.tsx"
      via: "app entrypoint render"
      pattern: "<PortfolioPage />|PortfolioPage"
    - from: "frontend/src/pages/PortfolioPage.tsx"
      to: "frontend/src/api/portfolio.ts"
      via: "TanStack Query read"
      pattern: "queryKey: \['portfolio'\]"
    - from: "frontend/src/features/portfolio/components/PortfolioSummaryCard.tsx"
      to: "frontend/src/features/portfolio/types.ts"
      via: "selected-base-currency total net worth props"
      pattern: "totalNetWorth|baseCurrency"
    - from: "frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx"
      to: "frontend/src/features/portfolio/types.ts"
      via: "breakdown DTO props"
      pattern: "ValuationBreakdown"
---

<objective>
Build the Phase 1 frontend and verification flow that presents backend-owned canonical portfolio values.

Purpose: Deliver the user-visible proof of PORT-01, PORT-02, and PORT-03 without violating the backend-as-truth rule.
Output: Portfolio page, UI tests, Playwright coverage, and a final human verification checkpoint.
</objective>

<execution_context>
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-CONTEXT.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-RESEARCH.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-UI-SPEC.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the portfolio page, settings form, and fast component/page coverage</name>
  <read_first>
    - frontend/src/features/portfolio/types.ts
    - frontend/src/api/portfolio.ts
    - frontend/src/api/settings.ts
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-CONTEXT.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-UI-SPEC.md
  </read_first>
  <files>frontend/src/App.tsx, frontend/src/features/settings/components/BaseCurrencyForm.tsx, frontend/src/features/portfolio/components/PortfolioSummaryCard.tsx, frontend/src/features/portfolio/components/PortfolioSection.tsx, frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx, frontend/src/pages/PortfolioPage.tsx, frontend/src/pages/PortfolioPage.test.tsx</files>
  <behavior>
    - Test 1: The page renders header, base-currency settings card, net-worth summary card, accounts section, and holdings section in that order.
    - Test 2: The summary card renders visible total net worth in the selected base currency from the canonical backend response.
    - Test 3: Rows show native and base values together by default.
    - Test 4: Saving base currency triggers backend mutation and query invalidation/refetch rather than client-side recomputation.
    - Test 5: Only one valuation breakdown is visible at a time and it shows original amount, price used, FX rate used, and converted result.
  </behavior>
  <action>Create `PortfolioPage.tsx` that follows the exact UI order from `01-UI-SPEC.md`: header, base-currency settings card, net-worth summary card, accounts section, holdings section. Implement `BaseCurrencyForm.tsx` with React Hook Form as the narrow settings command surface from research, a select control, and primary CTA text exactly `Save base currency`; on submit, call the settings API and invalidate/refetch the `['portfolio']` query per D-03 instead of recalculating values in React. Implement `PortfolioSummaryCard.tsx` so it renders the backend-provided `totalNetWorth` amount together with the selected `baseCurrency`, making the Phase 1 PORT-02 net-worth value visibly prominent in the UI. Implement reusable section and summary components that render both native and converted values together by default per D-04. Implement `ValuationBreakdownTrigger.tsx` as an icon-button-based lightweight disclosure showing `original amount`, `price used`, `FX rate used`, and `converted result`, with only one open breakdown at a time per D-05 and D-06. Add fast Vitest page/component coverage proving visible net-worth rendering, the save-and-refresh flow, section separation, and breakdown interaction. Include the UI copy from the spec for empty and error states where applicable.</action>
  <verify>
    <automated>npm run test:ui -- portfolio --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/PortfolioPage.tsx` renders accounts and holdings as separate sections.
    - `frontend/src/features/portfolio/components/PortfolioSummaryCard.tsx` visibly renders backend-provided `totalNetWorth` with the selected `baseCurrency` for PORT-02.
    - `frontend/src/features/settings/components/BaseCurrencyForm.tsx` uses React Hook Form and contains the exact CTA text `Save base currency`.
    - `frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx` renders fields for original amount, price used, FX rate used, and converted result.
    - `npm run test:ui -- portfolio --runInBand` exits 0.
  </acceptance_criteria>
  <done>PORT-01, PORT-02, and UI rendering for PORT-03 are visible in the Phase 1 page: canonical backend net worth appears in the selected base currency, the save flow refreshes backend-owned values, and rows expose native/base values plus breakdown details without frontend-owned valuation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright coverage for the end-to-end save-and-refresh workflow</name>
  <read_first>
    - frontend/src/pages/PortfolioPage.tsx
    - frontend/src/features/settings/components/BaseCurrencyForm.tsx
    - frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-UI-SPEC.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
  </read_first>
  <files>e2e/package.json, e2e/playwright.config.ts, e2e/tests/portfolio.spec.ts</files>
  <action>Create the Playwright workspace and an end-to-end spec that loads the portfolio page, changes base currency through the settings card, confirms the visible total net worth refreshes from the backend in the newly selected base currency for PORT-02, and verifies row-level valuation breakdown visibility without leaving the page. Keep this task focused on browser workflow proof; do not move component assertions that already belong in Vitest into Playwright.</action>
  <verify>
    <automated>npm run test:e2e -- portfolio</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/tests/portfolio.spec.ts` covers base-currency save plus portfolio refresh.
    - `e2e/tests/portfolio.spec.ts` asserts the summary card shows visible total net worth in the selected base currency.
    - `e2e/tests/portfolio.spec.ts` asserts accounts and holdings remain visible as separate sections.
    - `npm run test:e2e -- portfolio` exits 0.
  </acceptance_criteria>
  <done>Browser-level verification exists for the canonical save-and-refresh workflow and visible selected-base-currency net-worth rendering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the trust-oriented portfolio UI behavior</name>
  <read_first>
    - frontend/src/pages/PortfolioPage.tsx
    - frontend/src/features/settings/components/BaseCurrencyForm.tsx
    - frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-UI-SPEC.md
  </read_first>
  <files>frontend/src/features/settings/components/BaseCurrencyForm.tsx, frontend/src/features/portfolio/components/PortfolioSection.tsx, frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx, frontend/src/pages/PortfolioPage.tsx, e2e/tests/portfolio.spec.ts</files>
  <action>After Tasks 1 and 2 pass their automated coverage, perform a human review against the Phase 1 UI contract. Use this checkpoint only to confirm presentation fidelity and interaction clarity; do not use it to replace automated testing or backend verification.</action>
  <verify>
    <automated>npm run test:ui -- portfolio --runInBand && npm run test:e2e -- portfolio</automated>
  </verify>
  <acceptance_criteria>
    - The page shows header, settings card, summary card, accounts section, and holdings section in the specified order.
    - The summary card shows visible total net worth in the currently selected base currency.
    - At least one account row and one holding row show native and converted values together without extra clicks.
    - Opening a breakdown reveals original amount, price used, FX rate used, and converted result, and only one breakdown stays open.
  </acceptance_criteria>
  <what-built>Claude created the React portfolio page, React Hook Form base-currency save flow, visible selected-base-currency net-worth summary card, row-level breakdown interaction, and automated UI/e2e coverage.</what-built>
  <how-to-verify>
    1. Start the app with the backend and frontend dev commands created in this phase.
    2. Visit the local portfolio page URL served by Vite.
    3. Confirm the page order is header, base-currency card, net worth card, accounts section, holdings section.
    4. Confirm the summary card shows total net worth and the currently selected base currency together.
    5. Change the base currency, click `Save base currency`, and confirm the visible net-worth value refreshes after the save completes.
    6. Open one account breakdown and one holding breakdown separately; confirm only one stays open and each shows original amount, price used, FX rate used, and converted result.
  </how-to-verify>
  <done>Human verification confirms the implemented UI matches the trust-oriented Phase 1 contract, including visible selected-base-currency net worth, or concrete mismatches are reported for follow-up.</done>
  <resume-signal>Type "approved" if the UI matches the contract, or describe any mismatch that needs replanning/fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend form → settings API | User-selected base currency crosses from UI into persisted backend state |
| frontend query layer → portfolio API | Canonical finance data crosses into presentation code and must not be recomputed |
| interactive row disclosure → human trust judgment | Misrendered breakdown details could undermine confidence in totals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | T | `frontend/src/features/settings/components/BaseCurrencyForm.tsx` | mitigate | Use React Hook Form to submit only explicit `baseCurrency` payloads and refresh canonical data through query invalidation instead of local recomputation. |
| T-01-14 | T | `frontend/src/features/portfolio/components/PortfolioSection.tsx` and related UI | mitigate | Render backend DTO strings directly and avoid any frontend total/subtotal math helpers for canonical values. |
| T-01-15 | I | `frontend/src/features/portfolio/components/ValuationBreakdownTrigger.tsx` | mitigate | Render only the approved D-05 breakdown fields and avoid exposing internal IDs or raw ORM/debug payloads. |
</threat_model>

<verification>
Run `npm run test:ui -- portfolio --runInBand`, then `npm run test:e2e -- portfolio`, then complete the human verification checkpoint against the UI spec.
</verification>

<success_criteria>
- The Phase 1 portfolio page matches the UI contract structure.
- The summary card shows visible net worth in the selected base currency.
- Base-currency save flow refreshes canonical backend data.
- Native values, base values, and on-demand breakdown details are visible in the UI without client-side canonical recomputation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-domain-valuation-foundation/01-05-SUMMARY.md`
</output>
