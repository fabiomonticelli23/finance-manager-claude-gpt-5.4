---
phase: 01-portfolio-domain-valuation-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/seed.ts
  - backend/src/prisma/prisma.module.ts
  - backend/src/prisma/prisma.service.ts
  - backend/src/settings/dto/update-base-currency.dto.ts
  - backend/src/settings/settings.module.ts
  - backend/src/settings/settings.service.ts
  - backend/src/settings/settings.controller.ts
  - backend/src/app.module.ts
  - backend/test/settings.integration.spec.ts
autonomous: true
requirements:
  - PORT-01
must_haves:
  truths:
    - "User can save one app-wide base currency and later portfolio reads can consume that setting consistently."
    - "Base currency inputs outside the supported allowlist are rejected at the API boundary."
    - "Phase 1 seed data provides a deterministic starting base currency for valuation work."
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Settings persistence with explicit account/holding domain foundation"
    - path: "backend/src/settings/settings.controller.ts"
      provides: "HTTP contract for reading and updating the global base currency"
    - path: "backend/test/settings.integration.spec.ts"
      provides: "Automated proof that valid updates persist and invalid currency codes are rejected"
  key_links:
    - from: "backend/src/settings/settings.controller.ts"
      to: "backend/src/settings/settings.service.ts"
      via: "GET and PATCH handlers"
      pattern: "constructor\(private readonly settingsService"
    - from: "backend/src/settings/settings.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "Prisma settings table access"
      pattern: "baseCurrency"
---

<objective>
Create the app-wide base-currency settings foundation and database persistence layer required by Phase 1.

Purpose: Deliver PORT-01 and locked decision D-03 through a backend-owned setting rather than any frontend-only state.
Output: Prisma schema, seed data, Prisma module wiring, and tested settings endpoints for the global base currency.
</objective>

<execution_context>
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-CONTEXT.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-RESEARCH.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and prove the single app-wide base currency</name>
  <read_first>
    - backend/package.json
    - backend/src/app.module.ts
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-RESEARCH.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-CONTEXT.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
  </read_first>
  <files>backend/prisma/schema.prisma, backend/prisma/seed.ts, backend/src/prisma/prisma.module.ts, backend/src/prisma/prisma.service.ts, backend/src/settings/dto/update-base-currency.dto.ts, backend/src/settings/settings.module.ts, backend/src/settings/settings.service.ts, backend/src/settings/settings.controller.ts, backend/src/app.module.ts, backend/test/settings.integration.spec.ts</files>
  <behavior>
    - Test 1: `GET /settings/base-currency` returns the persisted currency code from the seeded settings row.
    - Test 2: `PATCH /settings/base-currency` with `{ "baseCurrency": "USD" }` persists and returns `USD`.
    - Test 3: `PATCH /settings/base-currency` with an unsupported code such as `BTC` returns 400 before service execution.
  </behavior>
  <action>Create `backend/prisma/schema.prisma` with explicit models for `AppSetting`, `Account`, and `Holding`, keeping accounts and holdings separate per D-01 and D-02 even if only the settings row is used immediately. Use explicit PostgreSQL decimal mappings from research for future finance fields: money values `@db.Decimal(24, 8)`, quantities `@db.Decimal(24, 12)`, unit prices `@db.Decimal(24, 8)`, and FX rates `@db.Decimal(24, 12)`. Add `backend/prisma/seed.ts` that inserts a single settings row with default `baseCurrency: 'EUR'` for deterministic Phase 1 behavior. Wire `PrismaModule` and `PrismaService`, then implement `UpdateBaseCurrencyDto`, `SettingsModule`, `SettingsService`, and `SettingsController` with `GET /settings/base-currency` and `PATCH /settings/base-currency` endpoints. Restrict accepted reporting currencies to `EUR`, `USD`, and `GBP` so the setting remains backend-owned and app-wide per D-03. Add `backend/test/settings.integration.spec.ts` using Supertest to prove valid updates persist and unsupported codes fail with HTTP 400. Do not introduce any frontend-owned override path or per-screen currency setting here.</action>
  <verify>
    <automated>npm run test:backend -- settings --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `backend/prisma/schema.prisma` contains separate `model Account` and `model Holding` definitions plus a settings model with `baseCurrency`.
    - Finance columns in `backend/prisma/schema.prisma` use the explicit Decimal precision/scale policy from `01-RESEARCH.md`.
    - `backend/prisma/seed.ts` inserts default base currency `EUR`.
    - `backend/src/settings/settings.controller.ts` defines `GET /settings/base-currency` and `PATCH /settings/base-currency`.
    - `backend/src/settings/dto/update-base-currency.dto.ts` contains only `EUR`, `USD`, and `GBP` as accepted values.
    - `backend/test/settings.integration.spec.ts` covers both valid and invalid currency updates.
    - `npm run test:backend -- settings --runInBand` exits 0.
  </acceptance_criteria>
  <done>PORT-01 backend behavior exists: one persisted app-wide base currency can be read, updated, and validated automatically, and the schema preserves the exact-decimal account/holding foundation required by later valuation work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local client → settings API | Untrusted base-currency input crosses into persistence logic |
| settings service → database | Persisted app-wide configuration affects every later valuation response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `backend/src/settings/dto/update-base-currency.dto.ts` | mitigate | Restrict `baseCurrency` to explicit allowlist `EUR|USD|GBP` and validate with Nest before service execution. |
| T-01-05 | T | `backend/prisma/schema.prisma` | mitigate | Declare explicit PostgreSQL Decimal precision/scale for finance fields so the exact-money storage contract is fixed before valuation logic is added. |
| T-01-06 | I | `backend/src/settings/settings.controller.ts` | mitigate | Return explicit DTO payloads only for `baseCurrency`; do not expose raw Prisma records or unrelated settings fields. |
</threat_model>

<verification>
Run `npm run test:backend -- settings --runInBand`. Confirm GET returns the seeded default currency, PATCH persists allowed values, and unsupported codes return 400.
</verification>

<success_criteria>
- A single persisted base currency exists in the database.
- The backend exposes tested GET/PATCH endpoints for that value.
- Currency validation blocks unsupported reporting codes at the HTTP boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-domain-valuation-foundation/01-02-SUMMARY.md`
</output>
