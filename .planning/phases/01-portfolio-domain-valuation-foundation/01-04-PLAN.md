---
phase: 01-portfolio-domain-valuation-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-03
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/vitest.config.ts
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/app/providers/QueryProvider.tsx
  - frontend/src/api/client.ts
  - frontend/src/api/settings.ts
  - frontend/src/api/portfolio.ts
  - frontend/src/features/portfolio/types.ts
  - frontend/src/App.test.tsx
autonomous: true
requirements:
  - PORT-01
  - PORT-03
must_haves:
  truths:
    - "The Phase 1 app shell can load canonical portfolio data through dedicated query/API boundaries instead of ad hoc client fetches or local finance math."
    - "The frontend is ready to render backend-owned accounts and holdings as separate canonical sections because it consumes the Phase 1 portfolio DTO contract directly."
    - "A fast automated UI command proves the frontend foundation is ready for selected-base-currency portfolio rendering without client-side canonical recalculation, per D-03."
  artifacts:
    - path: "frontend/src/app/providers/QueryProvider.tsx"
      provides: "Shared TanStack Query provider for canonical server-state reads"
    - path: "frontend/src/api/portfolio.ts"
      provides: "Frontend read boundary for GET /portfolio"
    - path: "frontend/src/App.test.tsx"
      provides: "Fast Vitest smoke coverage for the frontend scaffold and provider wiring"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/app/providers/QueryProvider.tsx"
      via: "app bootstrap provider wiring"
      pattern: "QueryProvider"
    - from: "frontend/src/api/settings.ts"
      to: "backend settings API"
      via: "base-currency mutation wrapper"
      pattern: "base-currency"
    - from: "frontend/src/features/portfolio/types.ts"
      to: "backend/src/portfolio/dto/portfolio-response.dto.ts"
      via: "mirrored DTO contract"
      pattern: "accounts|holdings"
---

<objective>
Create the frontend workspace, fast UI test harness, and canonical API/query boundaries for Phase 1.

Purpose: Establish a lightweight React execution surface that can later render backend-owned portfolio data while already enforcing server-state boundaries.
Output: React/Vite scaffold, TanStack Query provider, API client wrappers, DTO types, and a passing Vitest smoke test.
</objective>

<execution_context>
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/super/Documents/finance-manager-claude-gpt-5.4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-CONTEXT.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-RESEARCH.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-UI-SPEC.md
@.planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the frontend foundation and canonical server-state boundaries</name>
  <read_first>
    - package.json
    - frontend/package.json
    - backend/src/portfolio/dto/portfolio-response.dto.ts
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-RESEARCH.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-UI-SPEC.md
    - .planning/phases/01-portfolio-domain-valuation-foundation/01-VALIDATION.md
  </read_first>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/vitest.config.ts, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/app/providers/QueryProvider.tsx, frontend/src/api/client.ts, frontend/src/api/settings.ts, frontend/src/api/portfolio.ts, frontend/src/features/portfolio/types.ts, frontend/src/App.test.tsx</files>
  <behavior>
    - Test 1: The frontend app boots through Vite, wraps the app with `QueryProvider`, and renders the root app shell.
    - Test 2: The smoke test proves the app imports the typed portfolio/settings boundaries instead of bypassing them with ad hoc fetches.
    - Test 3: The frontend contract preserves separate `accounts` and `holdings` sections and string finance values from the backend DTO.
  </behavior>
  <action>Create the React 19 + Vite 8 frontend workspace with scripts wired into the root `test:ui` command, then add the minimal `main.tsx` and `App.tsx` shell needed for Phase 1. Create a shared `QueryProvider` using TanStack Query v5 and wrap the app with it from `frontend/src/main.tsx`. Add API wrappers in `frontend/src/api/client.ts`, `frontend/src/api/settings.ts`, and `frontend/src/api/portfolio.ts` for the canonical backend endpoints. Define `frontend/src/features/portfolio/types.ts` from the backend DTO contract in Plan 03 so the UI consumes canonical response shapes directly, keeping `accounts` and `holdings` separate per D-01 and D-02 and treating all finance values as strings. Add `frontend/src/App.test.tsx` as the fast Vitest smoke test so `npm run test:ui -- --runInBand` proves the app boots under `QueryProvider`, imports the typed API/query boundaries, and does not introduce any frontend-owned canonical valuation helper. Keep this plan focused on scaffold and boundaries only; the actual `PortfolioPage` and its detailed rendering belong to Plan 05.</action>
  <verify>
    <automated>npm run test:ui -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` exposes a non-watch test command consumed by root `test:ui`.
    - `frontend/src/app/providers/QueryProvider.tsx` creates a shared `QueryClientProvider`.
    - `frontend/src/api/portfolio.ts` exports a function that fetches `GET /portfolio`.
    - `frontend/src/api/settings.ts` exports a function that saves `baseCurrency`.
    - `frontend/src/features/portfolio/types.ts` mirrors backend DTO section names `accounts` and `holdings` with string finance values.
    - `frontend/src/App.test.tsx` passes under Vitest.
    - `npm run test:ui -- --runInBand` exits 0.
  </acceptance_criteria>
  <done>The frontend runtime and server-state boundaries are proven through a fast smoke path and are ready for the later portfolio page to render canonical backend responses without local finance math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend bootstrap → query layer | Canonical server-state provider wiring controls how portfolio data reaches the UI |
| frontend API wrappers → backend endpoints | Request/response contracts cross from presentation code into canonical backend data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T | `frontend/src/api/settings.ts` | mitigate | Submit only explicit `baseCurrency` payloads through a narrow API wrapper instead of ad hoc object mutation in components. |
| T-01-11 | T | `frontend/src/features/portfolio/types.ts` and consumers | mitigate | Keep finance values as backend-provided strings and avoid frontend total/subtotal math helpers for canonical values. |
| T-01-12 | D | `frontend/package.json` and `frontend/vitest.config.ts` | mitigate | Create an explicit non-watch `test:ui` command so quick UI feedback remains available during implementation. |
</threat_model>

<verification>
Run `npm run test:ui -- --runInBand`. Confirm the app boots, the smoke test passes, the app is wrapped in the shared query provider, and the typed API/query boundaries are imported from the frontend scaffold.
</verification>

<success_criteria>
- The frontend scaffold boots with a passing Vitest smoke test.
- Query/provider and API boundaries exist for canonical portfolio and settings calls.
- DTO contracts preserve backend-owned accounts/holdings separation and string finance values.
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-domain-valuation-foundation/01-04-SUMMARY.md`
</output>
